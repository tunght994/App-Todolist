import React from 'react'
import Grid from './Grid'

const Folder = () => {
    return (
        <div className='folder'>
            <Grid
                col={ }
                mdCol={ }
                smCol={ }
                gap={ }
            >

            </Grid>
        </div>
    )
}

export default Folder
